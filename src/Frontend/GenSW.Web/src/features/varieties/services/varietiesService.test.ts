import { afterEach, describe, expect, it, vi } from 'vitest'
import { invalidateSession, setAccessToken } from '../../../shared/http/sessionManager'
import { createVariedade, getVariedadeById, listVariedades, setVariedadeAtivo, updateVariedade } from './varietiesService'
const variedade = { id: 'variety-1', especieId: 'species-1', nome: 'Pelo curto', ativo: true, createdAtUtc: '2026-09-01T12:00:00Z', updatedAtUtc: '2026-09-01T12:00:00Z', especie: { id: 'species-1', nomeComum: 'Cão', ativo: true } }
function mockFetch(value: unknown) { setAccessToken('varieties-token'); const fetchMock = vi.fn((input: RequestInfo | URL, init?: RequestInit): Promise<Response> => { void input; void init; return Promise.resolve(new Response(JSON.stringify(value), { headers: { 'Content-Type': 'application/json' } })) }); vi.stubGlobal('fetch', fetchMock); return fetchMock }
afterEach(() => { invalidateSession(); vi.restoreAllMocks(); vi.unstubAllGlobals() })
describe('varietiesService', () => { it('envia operações autenticadas e contratos próprios', async () => { const fetchMock = mockFetch(variedade); await createVariedade({ especieId: 'species-1', nome: 'Pelo' }); await getVariedadeById('variety-1'); await updateVariedade('variety-1', { especieId: 'species-1', nome: 'Novo' }); await setVariedadeAtivo('variety-1', false); expect(new URL(String(fetchMock.mock.calls[0][0])).pathname).toBe('/api/v1/variedades'); expect(fetchMock.mock.calls[0][1]?.method).toBe('POST'); expect(new Headers(fetchMock.mock.calls[0][1]?.headers).get('Authorization')).toBe('Bearer varieties-token'); expect(new URL(String(fetchMock.mock.calls[1][0])).pathname).toBe('/api/v1/variedades/variety-1'); expect(fetchMock.mock.calls[2][1]?.method).toBe('PUT'); expect(fetchMock.mock.calls[3][1]?.method).toBe('PATCH') }); it('serializa todos os filtros de listagem', async () => { const fetchMock = mockFetch({ items: [], page: 2, pageSize: 50, totalItems: 0, totalPages: 0 }); await listVariedades({ page: 2, pageSize: 50, search: 'Pelo', especieId: 'species-1', ativo: false, sortBy: 'ativo', sortDirection: 'desc' }); expect(Object.fromEntries(new URL(String(fetchMock.mock.calls[0][0])).searchParams)).toEqual({ page: '2', pageSize: '50', search: 'Pelo', especieId: 'species-1', ativo: 'false', sortBy: 'ativo', sortDirection: 'desc' }) }) })
