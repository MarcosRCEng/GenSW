# NA-01 — Espécies Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar integralmente a issue Redmine #293 com cadastro autenticado de espécies, unicidade case-insensitive no PostgreSQL, lifecycle sem exclusão física e UI utilizável.

**Architecture:** Adicionar um módulo vertical `Species` paralelo a `People`, com entidade rica `Especie`, serviço e repositório específicos, mapeamento EF Core e índices PostgreSQL por `lower(...)`. Expor os casos de uso por controller autenticado e consumi-los em uma feature React sem novas dependências.

**Tech Stack:** .NET 8, C# 12, EF Core 8.0.10, Npgsql 8.0.10, PostgreSQL efêmero nos testes, ASP.NET Core, React 18.3.1, TypeScript 5.6.3, React Router 7.18.2, Vitest 3.2.7 e Testing Library.

**Spec:** `docs/superpowers/specs/2026-08-31-especies-design.md`

## Global Constraints

- Fonte primária: Redmine #293; Evolução #291 somente para regra global ausente.
- Branch exata: `feature/293-especies`, baseada em `main`/`origin/main` no commit `b11201061e99256f80f2dac52980a09b3a220c81`.
- Não implementar nenhuma parte das issues #294–#299.
- Não criar `Genero`, `Familia`, `Ordem`, `Subespecie`, `Sinonimos`, `Raca`, `Variedade`, `Animal`, propriedade, produção ou genética.
- Não adicionar MediatR, CQRS, `IRepository<T>`, Redux, Zustand, React Query, Axios, Formik ou React Hook Form.
- Não alterar migrations existentes; criar somente a migration `AddEspecies` e atualizar o snapshot gerado.
- Não criar colunas auxiliares com `Normalizado` no nome; a unicidade usa índices por expressão `lower(...)`.
- Normalização: `Trim`, uma única U+0020 para cada sequência de whitespace, comparação sem distinção de caixa e preservação de acentos.
- `NomeComum`: obrigatório, 1–200 caracteres após normalização e único.
- `NomeCientifico`: opcional, vazio vira `null`, máximo de 200 caracteres e único quando informado.
- Espécie inativa continua consultável, listável, editável e reativável.
- Não existe exclusão física nem endpoint `DELETE`.
- Todos os endpoints de espécies exigem autenticação.
- Seguir RED–GREEN–REFACTOR: cada código de produção nasce depois de um teste que falhou pelo motivo esperado.
- Por instrução específica do pedido, não criar commits de implementação intermediários; fazer o commit de implementação somente após todos os gates locais passarem. Commits puramente documentais permanecem separados.
- Não fazer merge nem excluir a branch.

---

### Task 1: Entidade de domínio e lifecycle

**Files:**
- Create: `src/Backend/GenSW.Domain/Species/Especie.cs`
- Create: `tests/GenSW.Domain.Tests/EspecieTests.cs`

**Interfaces:**
- Produces: `Especie.Criar(string nomeComum, string? nomeCientifico, DateTimeOffset nowUtc)`.
- Produces: `AlterarCadastro(string nomeComum, string? nomeCientifico, DateTimeOffset nowUtc)`.
- Produces: `Inativar(DateTimeOffset nowUtc)` e `Reativar(DateTimeOffset nowUtc)`.
- Produces read-only properties `Id`, `NomeComum`, `NomeCientifico`, `Ativo`, `CreatedAtUtc` e `UpdatedAtUtc`.

- [ ] **Step 1: Escrever os testes de domínio que falham**

Criar `EspecieTests.cs` com um teste por comportamento. O núcleo dos casos deve ser:

```csharp
[Fact]
public void Criar_normalizes_whitespace_preserves_case_and_defaults_to_active()
{
    var especie = Especie.Criar("  Cão\t  doméstico  ", "  Canis\n familiaris ", CreatedAt);

    Assert.NotEqual(Guid.Empty, especie.Id);
    Assert.Equal("Cão doméstico", especie.NomeComum);
    Assert.Equal("Canis familiaris", especie.NomeCientifico);
    Assert.True(especie.Ativo);
    Assert.Equal(CreatedAt, especie.CreatedAtUtc);
    Assert.Equal(CreatedAt, especie.UpdatedAtUtc);
}

[Theory]
[InlineData(null)]
[InlineData("")]
[InlineData("   ")]
public void Criar_rejects_missing_common_name(string? nomeComum)
    => Assert.ThrowsAny<ArgumentException>(() => Especie.Criar(nomeComum!, null, CreatedAt));

[Theory]
[InlineData(null)]
[InlineData("")]
[InlineData("   ")]
public void Criar_accepts_missing_scientific_name_as_null(string? nomeCientifico)
    => Assert.Null(Especie.Criar("Cão", nomeCientifico, CreatedAt).NomeCientifico);

[Fact]
public void AlterarCadastro_allows_inactive_species_to_be_edited()
{
    var especie = Especie.Criar("Cão", null, CreatedAt);
    especie.Inativar(CreatedAt.AddMinutes(1));

    especie.AlterarCadastro("  Cão doméstico ", " Canis familiaris ", CreatedAt.AddMinutes(2));

    Assert.False(especie.Ativo);
    Assert.Equal("Cão doméstico", especie.NomeComum);
    Assert.Equal("Canis familiaris", especie.NomeCientifico);
    Assert.Equal(CreatedAt.AddMinutes(2), especie.UpdatedAtUtc);
}
```

Adicionar casos separados para 201 caracteres em cada nome, exatamente 200 caracteres, edição sem mudança, inativação idempotente e reativação idempotente.

- [ ] **Step 2: Executar os testes e confirmar RED**

Run:

```powershell
dotnet test tests/GenSW.Domain.Tests/GenSW.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~EspecieTests
```

Expected: FAIL de compilação porque `GenSW.Domain.Species.Especie` ainda não existe. Esse é o motivo esperado; erros de sintaxe no teste devem ser corrigidos antes de prosseguir.

- [ ] **Step 3: Implementar a entidade mínima**

Criar `Especie.cs` no namespace `GenSW.Domain.Species`. Usar o mesmo padrão de construtor privado de `Pessoa`. A normalização deve ser centralizada e aplicada em criação e edição:

```csharp
private const int NomeMaxLength = 200;
private static readonly Regex WhitespacePattern = new(
    @"\s+",
    RegexOptions.Compiled | RegexOptions.CultureInvariant);

private static string NormalizeRequiredName(string value, string parameterName)
{
    ArgumentNullException.ThrowIfNull(value, parameterName);
    var normalized = WhitespacePattern.Replace(value.Trim(), " ");
    if (normalized.Length is < 1 or > NomeMaxLength)
    {
        throw new ArgumentException(
            "The common name must have between 1 and 200 characters.",
            parameterName);
    }

    return normalized;
}

private static string? NormalizeOptionalName(string? value, string parameterName)
{
    if (string.IsNullOrWhiteSpace(value)) return null;
    var normalized = WhitespacePattern.Replace(value.Trim(), " ");
    if (normalized.Length > NomeMaxLength)
    {
        throw new ArgumentException(
            "The scientific name cannot exceed 200 characters.",
            parameterName);
    }

    return normalized;
}
```

`Criar` gera `Guid`, define `Ativo = true` e inicializa os timestamps. `AlterarCadastro` não verifica `Ativo`; só atualiza campos e timestamp se o par de nomes realmente mudar. `Inativar` e `Reativar` retornam sem alterar timestamp quando o estado desejado já estiver aplicado.

- [ ] **Step 4: Executar RED–GREEN do domínio**

Run:

```powershell
dotnet test tests/GenSW.Domain.Tests/GenSW.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~EspecieTests
dotnet test tests/GenSW.Domain.Tests/GenSW.Domain.Tests.csproj --no-restore
```

Expected: todos os testes de `EspecieTests` e toda a suíte de domínio PASS, sem warnings novos.

---

### Task 2: Contratos e serviço da Application

**Files:**
- Create: `src/Backend/GenSW.Application/Species/CreateEspecieCommand.cs`
- Create: `src/Backend/GenSW.Application/Species/UpdateEspecieCommand.cs`
- Create: `src/Backend/GenSW.Application/Species/EspecieListQuery.cs`
- Create: `src/Backend/GenSW.Application/Species/EspecieSortField.cs`
- Create: `src/Backend/GenSW.Application/Species/EspecieResult.cs`
- Create: `src/Backend/GenSW.Application/Species/PagedEspecieResult.cs`
- Create: `src/Backend/GenSW.Application/Species/IEspecieRepository.cs`
- Create: `src/Backend/GenSW.Application/Species/IEspecieService.cs`
- Create: `src/Backend/GenSW.Application/Species/EspecieNotFoundException.cs`
- Create: `src/Backend/GenSW.Application/Species/EspecieDuplicateException.cs`
- Create: `src/Backend/GenSW.Application/Species/EspecieService.cs`
- Modify: `src/Backend/GenSW.Application/DependencyInjection.cs`
- Create: `tests/GenSW.Application.Tests/EspecieServiceTests.cs`
- Modify: `tests/GenSW.Application.Tests/DependencyInjectionTests.cs`

**Interfaces:**
- Consumes: `Especie` da Task 1.
- Produces: `IEspecieService` para o controller.
- Produces: `IEspecieRepository` para a infraestrutura.
- Produces: `EspecieDuplicateField.NomeComum` e `.NomeCientifico` para erro `409` específico.

- [ ] **Step 1: Escrever testes RED para casos de uso e DI**

Criar um `FakeEspecieRepository` in-memory e testes que provem:

```csharp
[Fact]
public async Task Create_rejects_a_normalized_common_name_conflict()
{
    var repository = new FakeEspecieRepository { CommonConflict = true };
    var service = CreateService(repository);

    var exception = await Assert.ThrowsAsync<EspecieDuplicateException>(() =>
        service.CreateAsync(new CreateEspecieCommand("  CÃO   DOMÉSTICO ", null)));

    Assert.Equal(EspecieDuplicateField.NomeComum, exception.Field);
    Assert.Equal("CÃO DOMÉSTICO", repository.LastCommonNameChecked);
    Assert.Null(repository.LastExcludedId);
}

[Fact]
public async Task Update_allows_an_inactive_species_and_excludes_its_id_from_conflict_checks()
{
    var repository = new FakeEspecieRepository();
    var especie = Especie.Criar("Cão", null, Now.AddDays(-1));
    especie.Inativar(Now.AddHours(-1));
    repository.Items.Add(especie);
    var service = CreateService(repository);

    var result = await service.UpdateAsync(
        especie.Id,
        new UpdateEspecieCommand("Cão doméstico", "Canis familiaris"));

    Assert.False(result.Ativo);
    Assert.Equal(especie.Id, repository.LastExcludedId);
    Assert.Equal("Cão doméstico", result.NomeComum);
}
```

Adicionar testes para create válido, científico conflitante, científico `null`, get existente/ausente, list com search normalizado, paginação inválida, sort inválido, update ausente, inativar, consultar inativa e reativar. Em `DependencyInjectionTests`, exigir `IPessoaService` e `IEspecieService` como serviços scoped.

- [ ] **Step 2: Confirmar RED da Application**

Run:

```powershell
dotnet test tests/GenSW.Application.Tests/GenSW.Application.Tests.csproj --no-restore --filter "FullyQualifiedName~EspecieServiceTests|FullyQualifiedName~DependencyInjectionTests"
```

Expected: FAIL de compilação por ausência dos tipos `Species` e do registro no container.

- [ ] **Step 3: Criar contratos exatos**

Usar estas assinaturas:

```csharp
public sealed record CreateEspecieCommand(string NomeComum, string? NomeCientifico);
public sealed record UpdateEspecieCommand(string NomeComum, string? NomeCientifico);

public enum EspecieSortField { NomeComum, NomeCientifico, Ativo, CreatedAtUtc }

public sealed record EspecieListQuery(
    int Page = 1,
    int PageSize = 25,
    string? Search = null,
    bool? Ativo = null,
    EspecieSortField SortBy = EspecieSortField.NomeComum,
    bool SortDescending = false);

public sealed record EspecieResult(
    Guid Id,
    string NomeComum,
    string? NomeCientifico,
    bool Ativo,
    DateTimeOffset CreatedAtUtc,
    DateTimeOffset UpdatedAtUtc);

public sealed record PagedEspecieResult(
    IReadOnlyList<EspecieResult> Items,
    int Page,
    int PageSize,
    int TotalItems,
    int TotalPages);

public sealed record EspecieListPage(
    IReadOnlyList<Especie> Items,
    int TotalItems);
```

`IEspecieService` deve expor:

```csharp
Task<EspecieResult> CreateAsync(
    CreateEspecieCommand command,
    CancellationToken cancellationToken = default);
Task<EspecieResult?> GetByIdAsync(
    Guid especieId,
    CancellationToken cancellationToken = default);
Task<PagedEspecieResult> ListAsync(
    EspecieListQuery query,
    CancellationToken cancellationToken = default);
Task<EspecieResult> UpdateAsync(
    Guid especieId,
    UpdateEspecieCommand command,
    CancellationToken cancellationToken = default);
Task<EspecieResult> SetActiveAsync(
    Guid especieId,
    bool ativo,
    CancellationToken cancellationToken = default);
```

`IEspecieRepository` deve expor:

```csharp
Task AddAsync(Especie especie, CancellationToken cancellationToken = default);
Task<Especie?> GetByIdReadOnlyAsync(Guid id, CancellationToken cancellationToken = default);
Task<Especie?> GetByIdForUpdateAsync(Guid id, CancellationToken cancellationToken = default);
Task<EspecieListPage> ListAsync(EspecieListQuery query, CancellationToken cancellationToken = default);
Task<bool> HasNomeComumConflictAsync(string nomeComum, Guid? excludingId = null, CancellationToken cancellationToken = default);
Task<bool> HasNomeCientificoConflictAsync(string nomeCientifico, Guid? excludingId = null, CancellationToken cancellationToken = default);
Task SaveChangesAsync(CancellationToken cancellationToken = default);
```

`EspecieDuplicateException` recebe `EspecieDuplicateField Field` e construtores com e sem `innerException`; o enum e as assinaturas são:

```csharp
public enum EspecieDuplicateField { NomeComum, NomeCientifico }

public sealed class EspecieDuplicateException : Exception
{
    public EspecieDuplicateException(EspecieDuplicateField field) : base(Message(field))
        => Field = field;

    public EspecieDuplicateException(
        EspecieDuplicateField field,
        Exception innerException) : base(Message(field), innerException)
        => Field = field;

    public EspecieDuplicateField Field { get; }

    private static string Message(EspecieDuplicateField field) => field switch
    {
        EspecieDuplicateField.NomeComum => "A species with the same common name already exists.",
        EspecieDuplicateField.NomeCientifico => "A species with the same scientific name already exists.",
        _ => throw new ArgumentOutOfRangeException(nameof(field)),
    };
}
```

`EspecieNotFoundException` recebe o `Guid` ausente e produz mensagem estável sem criar dependência de infraestrutura.

- [ ] **Step 4: Implementar `EspecieService` mínimo**

O serviço deve criar/alterar a entidade antes da pré-validação para que o repositório receba os nomes já tratados. O fluxo de validação é:

```csharp
private async Task EnsureUniqueAsync(
    Especie especie,
    Guid? excludingId,
    CancellationToken cancellationToken)
{
    if (await repository.HasNomeComumConflictAsync(
            especie.NomeComum, excludingId, cancellationToken))
    {
        throw new EspecieDuplicateException(EspecieDuplicateField.NomeComum);
    }

    if (especie.NomeCientifico is { } nomeCientifico &&
        await repository.HasNomeCientificoConflictAsync(
            nomeCientifico, excludingId, cancellationToken))
    {
        throw new EspecieDuplicateException(EspecieDuplicateField.NomeCientifico);
    }
}
```

Para update, obter entidade tracked, chamar `AlterarCadastro`, validar com `excludingId = especie.Id` e salvar. Não rejeitar `Ativo == false`. Validar `Page >= 1`, `1 <= PageSize <= 100` e `Enum.IsDefined(SortBy)`. Normalizar `Search` apenas com `Trim`, preservando o padrão existente de busca.

Registrar `services.AddScoped<IEspecieService, EspecieService>();` sem alterar os registros de `Pessoa`.

- [ ] **Step 5: Executar GREEN e regressão da Application**

Run:

```powershell
dotnet test tests/GenSW.Application.Tests/GenSW.Application.Tests.csproj --no-restore --filter "FullyQualifiedName~EspecieServiceTests|FullyQualifiedName~DependencyInjectionTests"
dotnet test tests/GenSW.Application.Tests/GenSW.Application.Tests.csproj --no-restore
```

Expected: todos os testes da Application PASS.

---

### Task 3: EF Core, repositório PostgreSQL e migration

**Files:**
- Modify: `src/Backend/GenSW.Infrastructure/Persistence/GenSWDbContext.cs`
- Create: `src/Backend/GenSW.Infrastructure/Species/EspecieRepository.cs`
- Modify: `src/Backend/GenSW.Infrastructure/DependencyInjection.cs`
- Create: `tests/GenSW.Infrastructure.Tests/EspeciePersistenceModelTests.cs`
- Create: `tests/GenSW.Infrastructure.Tests/EspecieRepositoryTests.cs`
- Modify: `tests/GenSW.Infrastructure.Tests/PersistenceConfigurationTests.cs`
- Create via EF tool: migration pair named `AddEspecies` under `src/Backend/GenSW.Infrastructure/Persistence/Migrations/`
- Modify via EF tool: `src/Backend/GenSW.Infrastructure/Persistence/Migrations/GenSWDbContextModelSnapshot.cs`

**Interfaces:**
- Consumes: `IEspecieRepository`, `EspecieListQuery` e exceptions da Task 2.
- Produces: `GenSWDbContext.Especies` e implementação `EspecieRepository`.
- Produces table `Especies`, check constraints e índices `UX_Especies_NomeComum_CaseInsensitive` e `UX_Especies_NomeCientifico_CaseInsensitive`.

- [ ] **Step 1: Escrever testes RED do modelo e registro de DI**

Em `EspeciePersistenceModelTests`, conferir tabela, nulabilidade, limites e default:

```csharp
var especie = context.Model.FindEntityType(typeof(Especie))!;
Assert.Equal("Especies", especie.GetTableName());
Assert.False(especie.FindProperty(nameof(Especie.NomeComum))!.IsNullable);
Assert.Equal(200, especie.FindProperty(nameof(Especie.NomeComum))!.GetMaxLength());
Assert.True(especie.FindProperty(nameof(Especie.NomeCientifico))!.IsNullable);
Assert.Equal(200, especie.FindProperty(nameof(Especie.NomeCientifico))!.GetMaxLength());
Assert.Equal(true, especie.FindProperty(nameof(Especie.Ativo))!.GetDefaultValue());
```

Conferir no design-time model as constraints `CK_Especies_NomeComum_Canonical` e `CK_Especies_NomeCientifico_Canonical`. Em `PersistenceConfigurationTests`, exigir o descriptor scoped `IEspecieRepository -> EspecieRepository`.

- [ ] **Step 2: Escrever testes RED de repositório em PostgreSQL real**

Usar o padrão `EphemeralPostgreSql` de `PessoaRepositoryTests`. Cobrir add/get, list/search/status/sort/paging, conflitos por caixa e exclusão do próprio `Id`:

```csharp
Assert.True(await repository.HasNomeComumConflictAsync("cÃO", null));
Assert.False(await repository.HasNomeComumConflictAsync("cÃO", existing.Id));
Assert.True(await repository.HasNomeCientificoConflictAsync("CANIS FAMILIARIS", null));
```

Adicionar um teste que grava duas entidades diretamente pelo repositório, sem pré-validação do serviço, e exige `EspecieDuplicateException` com o campo correto na segunda `SaveChangesAsync`. Fazer isso separadamente para nome comum e científico. Adicionar várias espécies com `NomeCientifico = null` para provar que são permitidas.

Adicionar teste de catálogo PostgreSQL após `MigrateAsync`:

```csharp
var indexes = await context.Database
    .SqlQueryRaw<string>("""
        SELECT indexname AS "Value"
        FROM pg_indexes
        WHERE schemaname = 'public' AND tablename = 'Especies'
        """)
    .ToListAsync();

Assert.Contains("UX_Especies_NomeComum_CaseInsensitive", indexes);
Assert.Contains("UX_Especies_NomeCientifico_CaseInsensitive", indexes);
```

Também consultar `pg_get_indexdef` e provar que as definições contêm `lower` e, no índice científico, `WHERE ("NomeCientifico" IS NOT NULL)`.

- [ ] **Step 3: Confirmar RED da infraestrutura**

Run:

```powershell
dotnet test tests/GenSW.Infrastructure.Tests/GenSW.Infrastructure.Tests.csproj --no-restore --filter "FullyQualifiedName~EspeciePersistenceModelTests|FullyQualifiedName~EspecieRepositoryTests|FullyQualifiedName~PersistenceConfigurationTests"
```

Expected: FAIL porque o DbSet, mapeamento, repositório, migration e DI ainda não existem.

- [ ] **Step 4: Implementar mapeamento EF e DI mínimos**

Adicionar `DbSet<Especie> Especies`. Mapear:

```csharp
builder.Entity<Especie>(especie =>
{
    especie.ToTable("Especies", table =>
    {
        table.HasCheckConstraint(
            "CK_Especies_NomeComum_Canonical",
            "\"NomeComum\" <> '' AND \"NomeComum\" = trim(\"NomeComum\") AND \"NomeComum\" NOT LIKE '%  %'");
        table.HasCheckConstraint(
            "CK_Especies_NomeCientifico_Canonical",
            "\"NomeCientifico\" IS NULL OR (\"NomeCientifico\" <> '' AND \"NomeCientifico\" = trim(\"NomeCientifico\") AND \"NomeCientifico\" NOT LIKE '%  %')");
    });
    especie.HasKey(entity => entity.Id);
    especie.Property(entity => entity.NomeComum).IsRequired().HasMaxLength(200);
    especie.Property(entity => entity.NomeCientifico).HasMaxLength(200);
    especie.Property(entity => entity.Ativo).IsRequired().HasDefaultValue(true);
    especie.Property(entity => entity.CreatedAtUtc).IsRequired();
    especie.Property(entity => entity.UpdatedAtUtc).IsRequired();
});
```

Registrar `services.AddScoped<IEspecieRepository, EspecieRepository>();`.

- [ ] **Step 5: Implementar consultas e tradução de conflito**

`HasNomeComumConflictAsync` e `HasNomeCientificoConflictAsync` devem deixar ambos os `lower` no SQL PostgreSQL:

```csharp
return context.Especies.AsNoTracking().AnyAsync(
    especie =>
        (excludingId == null || especie.Id != excludingId) &&
        especie.NomeComum.ToLower() == nomeComum.ToLower(),
    cancellationToken);
```

Para o científico, usar explicitamente:

```csharp
return context.Especies.AsNoTracking().AnyAsync(
    especie =>
        (excludingId == null || especie.Id != excludingId) &&
        especie.NomeCientifico != null &&
        especie.NomeCientifico.ToLower() == nomeCientifico.ToLower(),
    cancellationToken);
```

A busca usa `ILike` com `%...%` e o mesmo `EscapeLikePattern` de `PessoaRepository`. A ordenação deve ter `Id` como critério secundário determinístico.

Traduzir somente violações conhecidas:

```csharp
catch (DbUpdateException exception)
    when (exception.InnerException is PostgresException
    {
        SqlState: PostgresErrorCodes.UniqueViolation,
        ConstraintName: CommonNameIndex
    })
{
    throw new EspecieDuplicateException(
        EspecieDuplicateField.NomeComum,
        exception);
}
```

Repetir para `ScientificNameIndex`. Não converter outras falhas de banco em conflito de negócio.

- [ ] **Step 6: Gerar e ajustar somente a nova migration**

Run:

```powershell
dotnet tool restore
dotnet ef migrations add AddEspecies `
  --project src/Backend/GenSW.Infrastructure/GenSW.Infrastructure.csproj `
  --startup-project src/Backend/GenSW.API/GenSW.API.csproj `
  --output-dir Persistence/Migrations
rg --files src/Backend/GenSW.Infrastructure/Persistence/Migrations | rg 'AddEspecies|GenSWDbContextModelSnapshot'
```

No método `Up` da migration recém-gerada, depois de `CreateTable`, adicionar:

```csharp
migrationBuilder.Sql("""
    CREATE UNIQUE INDEX "UX_Especies_NomeComum_CaseInsensitive"
    ON "Especies" (lower("NomeComum"));
    """);
migrationBuilder.Sql("""
    CREATE UNIQUE INDEX "UX_Especies_NomeCientifico_CaseInsensitive"
    ON "Especies" (lower("NomeCientifico"))
    WHERE "NomeCientifico" IS NOT NULL;
    """);
```

O `Down` gerado deve apenas remover `Especies`; a remoção da tabela elimina os índices. Não editar nenhum arquivo de migration anterior a `AddEspecies`.

- [ ] **Step 7: Executar GREEN e regressão de infraestrutura**

Run:

```powershell
dotnet test tests/GenSW.Infrastructure.Tests/GenSW.Infrastructure.Tests.csproj --no-restore --filter "FullyQualifiedName~EspeciePersistenceModelTests|FullyQualifiedName~EspecieRepositoryTests|FullyQualifiedName~PersistenceConfigurationTests"
dotnet test tests/GenSW.Infrastructure.Tests/GenSW.Infrastructure.Tests.csproj --no-restore
```

Expected: novos testes e suíte completa de infraestrutura PASS, incluindo aplicação real da migration.

---

### Task 4: Contrato e endpoints autenticados da API

**Files:**
- Create: `src/Backend/GenSW.API/Contracts/Species/CreateEspecieRequest.cs`
- Create: `src/Backend/GenSW.API/Contracts/Species/UpdateEspecieRequest.cs`
- Create: `src/Backend/GenSW.API/Contracts/Species/UpdateEspecieStatusRequest.cs`
- Create: `src/Backend/GenSW.API/Contracts/Species/EspecieResponse.cs`
- Create: `src/Backend/GenSW.API/Contracts/Species/EspeciesListResponse.cs`
- Create: `src/Backend/GenSW.API/Controllers/EspeciesController.cs`
- Create: `tests/GenSW.API.Tests/EspeciesApiTests.cs`

**Interfaces:**
- Consumes: `IEspecieService` da Task 2.
- Produces: `/api/v1/especies` para o frontend.
- JSON fields: `id`, `nomeComum`, `nomeCientifico`, `ativo`, `createdAtUtc`, `updatedAtUtc`.

- [ ] **Step 1: Escrever testes RED de aceitação HTTP**

Seguir `PessoasApiTests`, criando cliente autenticado pelo helper existente. Cobrir em testes separados:

```csharp
using var anonymous = factory.CreateHttpsClient();
Assert.Equal(
    HttpStatusCode.Unauthorized,
    (await anonymous.GetAsync("/api/v1/especies")).StatusCode);

using var created = await client.PostAsJsonAsync("/api/v1/especies", new
{
    nomeComum = "  Cão   doméstico ",
    nomeCientifico = " Canis familiaris "
});
Assert.Equal(HttpStatusCode.Created, created.StatusCode);
```

Exigir create, list, get, update, inativar, get inativa, editar inativa e reativar. Exigir `400` para nome comum vazio e query inválida; `404` para os três endpoints por `Id`; `409` para duplicidade de cada nome por caixa/espaços; vários científicos nulos; `405 MethodNotAllowed` para `DELETE /api/v1/especies/{id}`.

No fluxo PostgreSQL de `EspeciesApiTests`, usar `EphemeralPostgreSql`, aplicar migrations e verificar a entidade persistida com `AsNoTracking`.

- [ ] **Step 2: Confirmar RED da API**

Run:

```powershell
dotnet test tests/GenSW.API.Tests/GenSW.API.Tests.csproj --no-restore --filter FullyQualifiedName~EspeciesApiTests
```

Expected: requests recebem `404` ou o teste não compila porque os contratos/controller ainda não existem.

- [ ] **Step 3: Criar contratos e controller mínimos**

Contratos:

```csharp
public sealed record CreateEspecieRequest(string NomeComum, string? NomeCientifico);
public sealed record UpdateEspecieRequest(string NomeComum, string? NomeCientifico);
public sealed record UpdateEspecieStatusRequest(bool Ativo);
public sealed record EspecieResponse(
    Guid Id, string NomeComum, string? NomeCientifico, bool Ativo,
    DateTimeOffset CreatedAtUtc, DateTimeOffset UpdatedAtUtc);
public sealed record EspeciesListResponse(
    IReadOnlyList<EspecieResponse> Items,
    int Page, int PageSize, int TotalItems, int TotalPages);
```

Controller:

```csharp
[ApiController]
[Authorize]
[Route("api/v1/especies")]
public sealed class EspeciesController(IEspecieService especies) : ControllerBase
```

Implementar `POST`, os dois `GET`, `PUT` e `PATCH {id:guid}/ativo`, seguindo `PessoasController`. Parsear `sortBy` apenas para `nomeComum`, `nomeCientifico`, `ativo` e `createdAtUtc`; parsear direção `asc|desc`.

Mapear exceptions:

```csharp
catch (EspecieNotFoundException) { return NotFound(); }
catch (EspecieDuplicateException exception)
{
    return Conflict(new ProblemDetails
    {
        Title = "Species already exists",
        Detail = exception.Field == EspecieDuplicateField.NomeComum
            ? "A species with the same common name already exists."
            : "A species with the same scientific name already exists.",
        Status = StatusCodes.Status409Conflict,
    });
}
catch (ArgumentException exception)
{
    return BadRequest(new ProblemDetails
    {
        Title = "Invalid species data",
        Detail = exception.Message,
        Status = StatusCodes.Status400BadRequest,
    });
}
```

- [ ] **Step 4: Executar GREEN e regressão da API**

Run:

```powershell
dotnet test tests/GenSW.API.Tests/GenSW.API.Tests.csproj --no-restore --filter FullyQualifiedName~EspeciesApiTests
dotnet test tests/GenSW.API.Tests/GenSW.API.Tests.csproj --no-restore
```

Expected: todos os testes da API PASS, sem rota `DELETE`.

---

### Task 5: Tipos, parser e cliente HTTP do frontend

**Files:**
- Create: `src/Frontend/GenSW.Web/src/features/species/types/species.ts`
- Create: `src/Frontend/GenSW.Web/src/features/species/services/speciesContractParsers.ts`
- Create: `src/Frontend/GenSW.Web/src/features/species/services/speciesContractParsers.test.ts`
- Create: `src/Frontend/GenSW.Web/src/features/species/services/speciesService.ts`
- Create: `src/Frontend/GenSW.Web/src/features/species/services/speciesService.test.ts`

**Interfaces:**
- Consumes: `/especies` relativo ao `API_BASE_URL` do `httpRequest` existente.
- Produces: `createEspecie`, `getEspecieById`, `listEspecies`, `updateEspecie`, `setEspecieAtivo`.
- Produces runtime-validated `Especie` e `EspeciesPage`.

- [ ] **Step 1: Escrever testes RED do parser**

Usar este objeto válido:

```typescript
const especie: Especie = {
  id: 'species-1',
  nomeComum: 'Cão doméstico',
  nomeCientifico: 'Canis familiaris',
  ativo: true,
  createdAtUtc: '2026-08-31T12:00:00Z',
  updatedAtUtc: '2026-08-31T12:00:00Z',
}
```

Exigir aceite de científico string ou `null`, rejeição de `id` vazio, nomes com tipo errado, `ativo` não booleano, datas inválidas e metadados de página inválidos.

- [ ] **Step 2: Escrever testes RED do cliente**

Mockar `httpRequest` conforme `peopleService.test.ts`. Exigir métodos/paths/bodies exatos e query:

```typescript
await listEspecies({
  page: 2,
  pageSize: 50,
  search: 'Canis',
  ativo: false,
  sortBy: 'nomeCientifico',
  sortDirection: 'desc',
})

expect(requestedUrl.searchParams.get('ativo')).toBe('false')
expect(requestedUrl.searchParams.get('sortBy')).toBe('nomeCientifico')
```

- [ ] **Step 3: Confirmar RED do frontend HTTP**

Run:

```powershell
npm test -- src/features/species/services/speciesContractParsers.test.ts src/features/species/services/speciesService.test.ts
```

Expected: FAIL porque os módulos `species` não existem.

- [ ] **Step 4: Implementar tipos, parser e cliente mínimos**

Tipos:

```typescript
export interface Especie {
  id: string
  nomeComum: string
  nomeCientifico: string | null
  ativo: boolean
  createdAtUtc: string
  updatedAtUtc: string
}

export type EspecieSortBy = 'nomeComum' | 'nomeCientifico' | 'ativo' | 'createdAtUtc'
export type SortDirection = 'asc' | 'desc'

export interface CreateEspecieRequest {
  nomeComum: string
  nomeCientifico?: string | null
}

export interface UpdateEspecieRequest {
  nomeComum: string
  nomeCientifico?: string | null
}

export interface UpdateEspecieStatusRequest {
  ativo: boolean
}

export interface EspeciesPage {
  items: Especie[]
  page: number
  pageSize: number
  totalItems: number
  totalPages: number
}

export interface ListEspeciesParams {
  page?: number
  pageSize?: number
  search?: string
  ativo?: boolean
  sortBy?: EspecieSortBy
  sortDirection?: SortDirection
}
```

O parser deve reutilizar as guardas `isRecord`, `isNonEmptyString`, `isIsoDateString` e `isIntegerAtLeast` de `peopleContractParsers.ts`, mantendo-as locais ao arquivo e emitindo mensagens específicas de espécie. O cliente usa `const SPECIES_ENDPOINT = '/especies'`, `authenticated: true` em todas as chamadas e estas assinaturas:

```typescript
export async function createEspecie(request: CreateEspecieRequest): Promise<Especie>
export async function getEspecieById(id: string): Promise<Especie>
export async function listEspecies(params?: ListEspeciesParams): Promise<EspeciesPage>
export async function updateEspecie(id: string, request: UpdateEspecieRequest): Promise<Especie>
export async function setEspecieAtivo(id: string, ativo: boolean): Promise<Especie>
```

- [ ] **Step 5: Executar GREEN do frontend HTTP**

Run:

```powershell
npm test -- src/features/species/services/speciesContractParsers.test.ts src/features/species/services/speciesService.test.ts
```

Expected: os dois arquivos PASS.

---

### Task 6: Listagem de espécies no navegador

**Files:**
- Create: `src/Frontend/GenSW.Web/src/features/species/pages/SpeciesListPage.tsx`
- Create: `src/Frontend/GenSW.Web/src/features/species/pages/SpeciesListPage.test.tsx`

**Interfaces:**
- Consumes: `listEspecies` e `setEspecieAtivo` da Task 5.
- Produces: página `/especies` com filtros, paginação e lifecycle.

- [ ] **Step 1: Escrever testes RED da listagem**

Mockar o serviço e testar:

- loading inicial `Carregando espécies…`;
- tabela com `Nome comum`, `Nome científico`, `Status`, `Cadastro` e `Ações`;
- busca por nome, filtro `Todos|Ativos|Inativos`, sort e direção;
- paginação e troca de page size;
- estado vazio `Nenhuma espécie encontrada.`;
- falha e botão `Tentar novamente`;
- ação `Inativar` e ação `Reativar` recarregando com os filtros atuais;
- link `Editar` presente também para registro inativo;
- bloqueio de duplo clique durante mudança de status.

Exemplo obrigatório:

```typescript
it('mantém edição disponível para espécie inativa', async () => {
  vi.mocked(listEspecies).mockResolvedValue({
    items: [{ ...speciesPage.items[0], ativo: false }],
    page: 1,
    pageSize: 25,
    totalItems: 1,
    totalPages: 1,
  })

  renderPage()

  expect(await screen.findByRole('link', { name: 'Editar' }))
    .toHaveAttribute('href', '/especies/species-1/editar')
})
```

- [ ] **Step 2: Confirmar RED da listagem**

Run:

```powershell
npm test -- src/features/species/pages/SpeciesListPage.test.tsx
```

Expected: FAIL porque `SpeciesListPage` ainda não existe.

- [ ] **Step 3: Implementar a listagem mínima seguindo `PeopleListPage`**

Copiar a estrutura visual consolidada, substituindo regras específicas de pessoa. Estado esperado:

```typescript
const [result, setResult] = useState<EspeciesPage | null>(null)
const [searchDraft, setSearchDraft] = useState('')
const [search, setSearch] = useState<string | undefined>()
const [ativo, setAtivo] = useState<boolean | undefined>()
const [sortBy, setSortBy] = useState<EspecieSortBy>('nomeComum')
const [sortDirection, setSortDirection] = useState<SortDirection>('asc')
const [page, setPage] = useState(1)
const [pageSize, setPageSize] = useState(25)
```

A tabela mostra `—` quando `nomeCientifico` é `null`. Sempre renderizar `Link to={`/especies/${especie.id}/editar`}`. Preservar proteção contra respostas assíncronas obsoletas com `isCurrent`, como em `PeopleListPage`.

- [ ] **Step 4: Executar GREEN da listagem**

Run:

```powershell
npm test -- src/features/species/pages/SpeciesListPage.test.tsx
```

Expected: todos os testes da listagem PASS.

---

### Task 7: Formulário, rotas protegidas e navegação

**Files:**
- Create: `src/Frontend/GenSW.Web/src/features/species/pages/SpeciesFormPage.tsx`
- Create: `src/Frontend/GenSW.Web/src/features/species/pages/SpeciesFormPage.test.tsx`
- Modify: `src/Frontend/GenSW.Web/src/routes/AppRoutes.tsx`
- Modify: `src/Frontend/GenSW.Web/src/routes/AppRoutes.test.tsx`
- Modify: `src/Frontend/GenSW.Web/src/features/auth/pages/AuthenticatedHomePage.tsx`

**Interfaces:**
- Consumes: create/get/update da Task 5.
- Produces: `/especies/nova` e `/especies/:id/editar` protegidas.
- Produces: link `Espécies` na home autenticada.

- [ ] **Step 1: Escrever testes RED do formulário**

Cobrir cadastro, edição ativa, edição inativa, loading, `404`, erro genérico, validação local e conflito `409`. Casos essenciais:

```typescript
fireEvent.change(screen.getByLabelText('Nome comum'), {
  target: { value: '  Cão doméstico  ' },
})
fireEvent.change(screen.getByLabelText('Nome científico'), {
  target: { value: 'Canis familiaris' },
})
fireEvent.click(screen.getByRole('button', { name: 'Salvar' }))

await waitFor(() => expect(createEspecie).toHaveBeenCalledWith({
  nomeComum: 'Cão doméstico',
  nomeCientifico: 'Canis familiaris',
}))
```

Para científico vazio, exigir `nomeCientifico: null`. Para espécie inativa carregada, campos e botão `Salvar` continuam habilitados. Para `HttpError` com status `409`, mostrar `Já existe uma espécie com esse nome comum ou científico.`.

- [ ] **Step 2: Escrever testes RED de rotas**

Adicionar mock do `speciesService` em `AppRoutes.test.tsx`. Testar acesso anônimo redirecionado para login e usuário autenticado renderizando listagem, nova espécie e edição. Testar clique no link `Espécies` da home.

- [ ] **Step 3: Confirmar RED do formulário e rotas**

Run:

```powershell
npm test -- src/features/species/pages/SpeciesFormPage.test.tsx src/routes/AppRoutes.test.tsx
```

Expected: FAIL por páginas/rotas ausentes.

- [ ] **Step 4: Implementar formulário mínimo**

Seguir `PeopleFormPage`, removendo tipo e nome fantasia. Regras de submit:

```typescript
const normalizedCommonName = nomeComum.trim().replace(/\s+/g, ' ')
const normalizedScientificName = nomeCientifico.trim().replace(/\s+/g, ' ')

if (normalizedCommonName.length < 1 || normalizedCommonName.length > 200) {
  setNomeComumError(true)
  return
}

if (normalizedScientificName.length > 200) {
  setNomeCientificoError(true)
  return
}

const request = {
  nomeComum: normalizedCommonName,
  nomeCientifico: normalizedScientificName || null,
}
```

Em edição, carregar por `id`, inclusive quando `ativo === false`, e chamar `updateEspecie`. Após sucesso, navegar para `/especies`. Importar e usar `isHttpError(error)` antes de distinguir `error.status === 404` e `409`; demais erros recebem mensagem genérica.

- [ ] **Step 5: Registrar rotas e navegação**

Adicionar imports e rotas dentro de `ProtectedRoute`:

```tsx
<Route element={<SpeciesListPage />} path="/especies" />
<Route element={<SpeciesFormPage />} path="/especies/nova" />
<Route element={<SpeciesFormPage />} path="/especies/:id/editar" />
```

Na seção `Cadastros` da home, manter `Pessoas` e adicionar link irmão `Espécies` para `/especies`, usando o mesmo estilo.

- [ ] **Step 6: Executar GREEN e toda a suíte frontend**

Run:

```powershell
npm test -- src/features/species/pages/SpeciesFormPage.test.tsx src/routes/AppRoutes.test.tsx
npm test
```

Expected: testes novos e os 100 testes de baseline, agora acrescidos dos testes de espécies, PASS.

---

### Task 8: Gates finais, auditoria, commit, push, PR, CI e Redmine

**Files:**
- Inspect: todos os arquivos alterados contra `main`.
- Update external: Redmine #293 somente após conclusão técnica.

**Interfaces:**
- Produces: branch pronta para revisão, PR para `main`, evidências de CI e roteiro de homologação.

- [ ] **Step 1: Restaurar e executar gates backend**

Run:

```powershell
dotnet restore GenSW.sln
dotnet build GenSW.sln --configuration Release --no-restore
dotnet test GenSW.sln --configuration Release --no-build --no-restore
```

Expected: restore, build e todas as suítes backend PASS, sem warnings novos.

- [ ] **Step 2: Executar gates frontend**

O `npm ci` já foi executado no worktree e `package.json`/`package-lock.json` não devem mudar; não repeti-lo salvo se `node_modules` for removido.

Run:

```powershell
npm test
npm run lint
npm run build
```

Workdir: `src/Frontend/GenSW.Web`.

Expected: testes, lint e build PASS.

- [ ] **Step 3: Validar migration e diff**

Run:

```powershell
dotnet ef migrations script --idempotent `
  --project src/Backend/GenSW.Infrastructure/GenSW.Infrastructure.csproj `
  --startup-project src/Backend/GenSW.API/GenSW.API.csproj `
  --output artifacts/AddEspecies.idempotent.sql
git diff --check
git status --short
git diff --name-status main
git diff --stat main
git diff main -- src/Backend/GenSW.Infrastructure/Persistence/Migrations
```

Expected:

- script gerado sem erro e contendo tabela, constraints, defaults e os dois índices;
- somente a nova migration e o snapshot mudam na pasta de migrations;
- nenhum arquivo de migration antigo modificado;
- nenhum arquivo temporário, segredo ou dependência nova;
- nenhuma referência de implementação a `Raca`, `Variedade`, `Animal` ou issues #294–#299;
- `git diff --check` sem saída.

Remover apenas o script gerado em `artifacts/` se ele não estiver ignorado; o diretório já é ignorado pelo repositório.

- [ ] **Step 4: Fazer o único commit de implementação após PASS**

Run:

```powershell
git add -- src tests docs/superpowers/plans/2026-08-31-especies.md
git diff --cached --check
git diff --cached --stat
git commit -m "feat: implement species management"
```

Expected: commit criado somente depois de todos os gates PASS. O commit documental `0f3f943` permanece separado.

- [ ] **Step 5: Fazer push e criar PR sem merge**

Run:

```powershell
git push -u origin feature/293-especies
gh pr create `
  --base main `
  --head feature/293-especies `
  --title "NA-01 — Espécies (Redmine #293)" `
  --body "Implementa verticalmente o cadastro de espécies da Redmine #293. Inclui domínio, EF Core/PostgreSQL, migration, Application, API autenticada, frontend e testes. Homologação manual e merge permanecem pendentes."
```

Se `gh` não estiver autenticado ou disponível, registrar exatamente a falha e preservar branch/commit/push; não inventar URL de PR.

- [ ] **Step 6: Acompanhar required checks**

Run:

```powershell
gh pr checks --watch
```

Expected: checks `Backend` e `Frontend` PASS. Se falharem, classificar a causa, reproduzir localmente, aplicar correção mínima com novo ciclo TDD, repetir todos os gates, criar commit corretivo e fazer push. Não fazer merge.

- [ ] **Step 7: Atualizar Redmine #293 para `Em validação`**

Usar `REDMINE_API_KEY` somente no cabeçalho `X-Redmine-API-Key`. Registrar branch, commits, URL do PR, testes, builds, lint, migration, CI, achados e homologação manual pendente. Alterar apenas #293 para status `Em validação`; não modificar #294–#299 e não marcar `Concluído`.

- [ ] **Step 8: Entregar roteiro de homologação**

Entregar ao desenvolvedor:

1. autenticar e acessar `Espécies`;
2. confirmar listagem, filtros e estado vazio/loading;
3. criar espécie somente com nome comum;
4. criar espécie com nome científico;
5. editar ambos os nomes;
6. inativar e confirmar que permanece listável, consultável e editável;
7. reativar;
8. tentar duplicidade de nome comum com caixa/espaços diferentes;
9. tentar duplicidade de nome científico com caixa/espaços diferentes;
10. confirmar que `Cão` e `Cao` são tratados como nomes distintos.
